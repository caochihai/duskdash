# Infrastructure Agent Instructions

Follow infrastructure-specific deployment, environment, and operations rules here.

Rules:

- Keep infrastructure code inside `infra/`.
- Keep Terraform code inside `infra/terraform/`.
- Do not commit real credentials, secrets, state files, or local variable files.
