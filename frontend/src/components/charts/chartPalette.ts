/**
 * Palette chart — re-export từ theme để component charts có một điểm import
 * thống nhất mà không phá vỡ nguyên tắc "token nằm trong src/theme".
 */
export { shbChartPalette, shbChartTheme } from '@/theme/chartTheme';
