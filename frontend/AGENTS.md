# Frontend Agent Instructions

Follow frontend-specific architecture, UI conventions, component rules, and styling rules here.

Rules:

- Keep frontend logic inside `frontend/`.
- Do not modify backend code unless the task explicitly requires it.
- Read relevant business documentation before changing user-facing behavior.
