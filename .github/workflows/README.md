# GitHub Workflows

Put GitHub Actions workflow files here.

Suggested future workflows:

- `ci.yml`: Run lint, tests, and builds.
- `deploy.yml`: Deploy application services.
- `terraform.yml`: Validate and plan Terraform changes.
